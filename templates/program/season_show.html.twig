{% extends "base.html.twig" %}
{% block title %}saison {{season.number}} {% endblock %}
{% block body %}
    <div class="media">
        <h1 class="mt-0">{{ season.program.title }}</h1>
        
        <img class="align-self-start mr-3" src="{{season.program.poster}}" alt="{{ season.program.title }} poster">
        <div class="media-body">
            <p>sortie : {{season.year}}</p>
            <p>pays : {{season.program.country}}</p>
            <p>Catégorie : {{ season.program.category.name }}</p>
            <p>{{ season.program.synopsis }}</p>
            <p>saison : {{season.number}}</p>
            <p>description de la saison : {{season.description}}</p>
        </div>
        <div class="media-body">
        {% for episode in episodes %}
            <p> episode : {{episode.number}}</p>
            <p> titre : {{episode.title}}</p>
            <p> synopsis : {{episode.synopsis}}</p>
            <p><a href="{{ path("program_episode_show", {programId: program.id, seasonId: season.id, episodeId: episode.id }) }}">Voir la fiche de l'episode {{ episode.number}}</a></p>

        {% endfor %}
        </div>
    </div>
{% endblock %}